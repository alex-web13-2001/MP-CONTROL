<clickhouse>
  <users>
    <default>
      <!-- Allow access from all networks (Docker containers) -->
      <networks>
        <ip>::/0</ip>
        <ip>0.0.0.0/0</ip>
      </networks>
      <!-- Empty password (as per original setup) -->
      <password></password>
    </default>
  </users>
</clickhouse>
